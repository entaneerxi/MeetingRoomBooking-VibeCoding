@model MeetingRoomBooking.Models.Room

@{
    ViewData["Title"] = "Delete Room";
}

<div class="container">
    <h1>Delete Room</h1>
    
    <div class="alert alert-danger">
        <h4>Are you sure you want to delete this room?</h4>
        <p>Deleting this room will remove all its bookings from the system.</p>
    </div>
    
    <div class="card">
        <div class="card-header bg-light">
            <h5>@Model.Name</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Room Number</dt>
                <dd class="col-sm-9">@Model.RoomNumber</dd>
                
                <dt class="col-sm-3">Floor</dt>
                <dd class="col-sm-9">@Model.FloorNumber</dd>
                
                <dt class="col-sm-3">Capacity</dt>
                <dd class="col-sm-9">@Model.Capacity people</dd>
                
                <dt class="col-sm-3">Has Projector</dt>
                <dd class="col-sm-9">@(Model.HasProjector ? "Yes" : "No")</dd>
                
                <dt class="col-sm-3">Has Video Conference</dt>
                <dd class="col-sm-9">@(Model.HasVideoConference ? "Yes" : "No")</dd>
                
                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@Model.Description</dd>
            </dl>
        </div>
        <div class="card-footer">
            <form asp-action="Delete">
                <input type="hidden" asp-for="RoomId" />
                <button type="submit" class="btn btn-danger">Delete</button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>